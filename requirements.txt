# Country Relocation and Culture Guide

Country Relocation and Culture Guide is a Streamlit app for exploring country facts, comparing places, saving a shortlist, and creating AI-assisted travel, study, or relocation guides.

## Setup

1. Create and activate a virtual environment.
2. Install dependencies with `python -m pip install -r requirements.txt`.   
6. Start the app with `streamlit run app.py`.